const app = require('../app');
const config = require('../config');

const _port = (config.PORT || 3000);

app.listen(_port, () => {
  console.log('App is listening to requests on port:', _port);
});
